# Drushe — Handoff / Continue-Here

מסמך המשכיות. בסשן חדש: קרא אותי קודם, ותמשיך מהמצב למטה.
**עדכן אותי בכל פעם שמסיימים משהו משמעותי.**

## מה זה
Drushe — אפליקציית שיעורים פרטיים שבה בני נוער (14–18) מלמדים ילדים, בפיקוח
הורים מלא. קובץ יחיד `index.html` (~13k שורות) + Firebase RTDB + Netlify
(https://kidemy-app.netlify.app). עטיפת Capacitor ל-Android תחת `mobile/`.

## כללי עבודה קבועים (חשוב!)
- **עונים בעברית תמיד.**
- **המשתמש עובד אך ורק מהסלולרי (אנדרואיד).** אין לו גישה למחשב. כל הוראה חייבת
  להיות ניתנת לביצוע בנייד; קבצים שצריך למסור לו — עדיף כעמוד Artifact עם
  התמונות מוטמעות (לחיצה ארוכה → שמור תמונה), כי כרטיסי הקבצים בצ'אט לא תמיד
  מאפשרים הורדה אצלו.
- **אין להשתמש ב-picker/לולאת שאלות אינטראקטיבית** (AskUserQuestion) — לשאול בטקסט רגיל.
- **מעלים APP_VERSION בכל תיקון** (המשתמש מוודא לפי המספר שהגיע למכשיר).
  חריג: שינוי שלא מגיע למכשיר כלל (קונפיג בנייה, CI, gitignore) — אין טעם להעלות,
  זה רק היה מבלבל אותו כשהוא מחפש את המספר החדש.
- **לא דוחפים ולא ממזגים בלי אישור מפורש ("כן").** פיתוח בענף
  `claude/handoff-continuation-4ec67t`, מיזוג ל-`main` רק אחרי "כן".
  Netlify מפרסם מ-`main`.
- לפני עריכה — קוראים את הקוד. אחרי תיקון — מסבירים את שורש הבעיה.

## מצב נוכחי
- גרסה: **APP_VERSION = 4.9.92**. main על `b5a81e9`.
- הענף `claude/handoff-continuation-4ec67t` מסונכרן עם main. ממשיכים עליו.
- חוקי ה-DB נפרסים אוטומטית ובהצלחה (הצינור תוקן — ראה למטה).

## 🐛 דף שחור ואחריו רילס — שורש נמצא, תוית טעינה נוספה (20.8.2026, עודכן 24.8.2026)

**הדיווח המקורי:** לפעמים בפתיחת האפליקציה נפתחים שני מסכים — אחד שחור עם
"Drushe" באמצע, ואחריו הרילס. המשתמש הדגיש שזה קורה **אחרי מיזוג/פריסה**,
ושחץ החזרה בראש המסך "שייך לפתיחה ולא לרילס".

**מה כבר נשלל:**
- ❌ שחזור המסך האחרון מהאחסון — `_restoreRegSession` שומר רק פרטי הרשמה.
  `S.screen` תמיד מתחיל ב-`splash`, ואין שום `localStorage`/`sessionStorage`
  ששומר את המסך הנוכחי (נבדק ב-24.8: `grep` על כל קריאות ה-setItem בקובץ).
- ❌ האונבורדינג, `DEMO_MODE`, מסך הפתיחה/הבטיחות — כמו קודם.

**✅ השורש נמצא ב-24.8.2026, ודאי ב-100% (לא ניחוש):** דף השחור עם "Drushe"
המהבהב הוא מצב הטעינה של מסך הרילס (`!discoverFeedLoaded||discoverFeedLoading`).
חיפוש מלא בקובץ מראה **מקום קריאה יחיד בכל האפליקציה** ל-`go('discover')` —
בתוך `openDiscover()`, שנקראת אך ורק מהכפתור העגול הצבעוני במסך הבית
(index.html:8712, ה"Drils"). אין ניווט אוטומטי, אין deep-link, אין שחזור
מצב — מבחינת הקוד **אי אפשר** להגיע למסך הזה בלי לחיצה על הכפתור הזה.

**למה זה "מרגיש" כאילו זה קורה מעצמו בפתיחה:** ב-`loadDiscoverFeed()` יש
טעינה מוקדמת ברקע (index.html:13542, `loadDiscoverFeed()` ללא `await`) שרצה
אוטומטית מיד אחרי כל התחברות — כולל כניסת אורח (`browseAsGuest` →
`signInAnonymously`, ולכן `currentUser` קיים גם בלי הרשמה אמיתית). אם לוחצים
על הכפתור העגול **בחלון הזמן הקצר** לפני שהטעינה ברקע הסתיימה (סביר מאוד
דווקא אחרי פתיחת האפליקציה, כשגוללים ובוחנים מה חדש) — רואים את מסך הטעינה
השחור. אם לוחצים כמה שניות אחר כך, הטעינה כבר הסתיימה והמעבר מיידי. זה
מסביר גם את ה"לפעמים" וגם את הקורלציה עם "אחרי פריסה" (בודקים את האפליקציה
בפעילות, לוחצים על הרבה כפתורים בפעם הראשונה).

**מה שהוא לא:** לא באג ניווט. הכפתור פשוט נראה כמו לוגו/אווטאר דקורטיבי
(טבעת גרדיאנט צבעונית + אייקון האפליקציה + תג ▶ קטן) ולא כמו כפתור פיצ'ר,
ולכן קל ללחוץ עליו בטעות בלי לזהות שזו פעולה.

**✅ תוקן (24.8.2026, APP_VERSION 4.11.2):** נוספה כותרת "טוען דרילס..." מתחת
ללוגו המהבהב במסך הטעינה, כדי שלעולם לא ייראה כמו מסך שקרס — בין אם הלחיצה
הייתה מכוונת ובין אם בטעות. שינוי תצוגה בלבד, לא נגע בלוגיקת הטעינה עצמה.

⬜ **לוודא במכשיר:** שהתוית מופיעה, ושתחושת "המסך קרס" נעלמה גם כשבאמת
לוחצים על הכפתור העגול בכוונה.

**✅ נעול ב-CI (24.8.2026)** כדי שהחקירה הזו לא תיפתח שוב מאפס בעתיד:
- `scripts/validate.sh` — בדיקת SSOT חדשה שסופרת שיש **בדיוק** מקום קריאה
  אחד ל-`go('discover')` בכל הקובץ. אם מישהו יוסיף עוד מקום שמנווט לדרילס
  בלי לעבור דרך השומר שב-`openDiscover()`, הדיפלוי ייכשל.
- `tests/e2e/discover-gate.spec.js` — שלושה טסטים: (1) עלייה נקייה מציגה את
  מסך ה-splash ולעולם לא את מסך טעינת הדרילס, (2) `Discover()` מסרב לרנדר
  בלי `currentUser` גם אם `S.screen` נדחף אליו ישירות (מדמה תקלה עתידית
  שדוחפת למסך הזה בעקיפין), (3) גלישת אורח מגיעה לדף הבית בלי שדרילס
  מופיע. רץ אוטומטית ב-CI על כל PR/פוש (`test.yml`) וגם כשער לפני דיפלוי
  ל-main (`deploy.yml`).

## איפוס סיסמה — הוחלט (20.8.2026)

מורה דיווח שלחץ "שכחתי סיסמה" ולא קיבל מייל. השורש: **כל החשבונות** —
מורים ותלמידים כאחד, מסלול הרשמה אחד — נוצרים ב-Firebase עם כתובת מומצאת
`<username>@kidemy.app`, שאין לה תיבת דואר. `sendPasswordResetEmail` עליה
**מצליח** (החשבון קיים), ולכן המסך הצהיר "נשלח קישור" בזמן שהמייל הלך
לשומקום.

**התיקון:** `netlify/functions/password-reset.js` מאתר את החשבון, מייצר
קישור עם ה-admin SDK, ושולח אותו ל-`parentEmail`. הקישור לעולם לא חוזר
לקורא, התשובה זהה בין אם החשבון קיים ובין אם לא (אחרת אפשר למנות שמות של
קטינים), ומוגבל ל-5 בקשות ל-10 דקות לכל IP.

✅ **הוגדר ואומת מקצה לקצה (22.8.2026).** מייל איפוס אמיתי הגיע לתיבת ההורה.

מה שהתברר כשנכנסנו סוף-סוף ל-Netlify: **אף אחד** מארבעת משתני EmailJS לא
היה מוגדר שם — לא רק המפתח הפרטי. הפונקציה שלחה בקשה עם
`service_id: undefined` ונכשלה עוד לפני שהמפתח בכלל היה רלוונטי. ההנחה
שהחסר היחיד הוא `EMAILJS_PRIVATE_KEY` הייתה שגויה, כי היא נבעה מקריאת הקוד
ולא מבדיקה של מה שבאמת מוגדר בשרת.

ארבעת המשתנים בסקופ `builds + functions + runtime`:
`EMAILJS_SERVICE_ID`, `EMAILJS_TEMPLATE_ID`, `EMAILJS_PUBLIC_KEY` (שלושתם
גלויים ממילא ב-`index.html`), ו-`EMAILJS_PRIVATE_KEY` **כסודי**
(`Contains secret values`) ב-Production, Deploy Previews ו-Branch deploys.

שני דברים שעלו בדרך ושווה לזכור:
- Netlify **מונע** `All scopes` ממשתנה סודי — הוא נופל אוטומטית ל-Specific
  בלי `Post processing`. זה תקין ולא צריך לתקן.
- ב-EmailJS חייב להיות ✅ על **Account → Security → "Allow EmailJS API for
  non-browser applications"**. בלי זה הקריאה נחסמת גם עם מפתח תקין, ושום
  דיפלוי לא יעזור.

לא נדרש דיפלוי — הפונקציה קוראת את משתני הסביבה בזמן ריצה.

### ההחלטה: איפוס עובר דרך ההורה, ולא מוסיפים מייל אישי למורים

הועלתה אפשרות להוסיף שדה מייל אישי אופציונלי למורים, כדי שמורה בן 17 לא
יצטרך את ההורה כדי לחזור לחשבון. **נדחתה במפורש על ידי בעל האפליקציה.**

המשמעות שצריך לחיות איתה: מורה שאיבד סיסמה תלוי בהורה שלו, ואם ההורה החליף
מייל או לא בודק אותו — הוא תקוע. הגיבוי לכך הוא ההתראה בפאנל: כשאין תיבה
שמישהו קורא, המנהל מקבל בקשה ומאפס ידנית.

**לא לפתוח מחדש בלי שהמשתמש יבקש.**

## ביקורת מקיפה — 19.8.2026

הורצו **שבע ביקורות מקבילות** (אבטחה, בטיחות קטינים, דאטה, עיצוב, קוד, מוצר/חנויות,
וביצועים+תשתית שנעשו ידנית). **166 ממצאים: 50 קריטיים, 56 גבוהים, 36 בינוניים.**

הדוחות המלאים נכתבו ל-`scratchpad/audit-*.md` (2,494 שורות, לא ב-git — סקראצ'פד
של הסשן). דוח מסכם קריא: https://claude.ai/code/artifact/49957c08-2a8c-4ce4-952b-72dc621865f0

**שלושה תוקנו ומוזגו (PR #89):**

1. **שער הקריאה של הצ'אט אישר את עצמו.** `messages/$chatId` נתן קריאה למי שרשום
   ב-`chatParticipants/$chatId` — צומת שהרשאת הכתיבה שלו הייתה `auth != null`.
   כל אחד כתב את עצמו פנימה וקרא כל שיחה פרטית בין קטינים. השרשרת לא דרשה כלום:
   `browseAsGuest()` → `signInAnonymously()`, `teachers` קריא לכל מחובר ונותן את
   כל ה-uid, ו-`getChatId` הוא `[a,b].sort().join('_')` — נגזר, לא סודי.
   **התיקון:** החברות נגזרת מה-`chatId` עצמו (`beginsWith`/`endsWith`), שאי אפשר
   לזייף. `chatParticipants` כבר לא בסיס להרשאה והפך מיותר — אפשר להסיר אותו
   מהקוד (3 מקומות: 3176, 3177, 4060) בקומיט נפרד.
   כבונוס: הוסרה הרשאת הכתיבה הרחבה ברמת השיחה, ולכן **חוק החסימה שהיה מת רץ עכשיו**.

2. **אי אפשר היה להזמין שיעור מאף מורה עם לוח.** `_dk` גזר מפתח תאריך מ-UTC,
   והעורך בונה תא כחצות מקומית — שהיא היום הקודם ב-UTC. מורה לחץ 17/6, נשמר 16/6.
   צד התלמיד היה נכון רק במקרה ושבור בין חצות ל-02:00. **התיקון:** גזירה מקומית
   אחת בשני מקומות השימוש.
   ⬜ **לוחות קיימים לא עברו מיגרציה — במכוון.** מי שעבר מהפורמט השבועי הישן עשוי
   להחזיק תערובת מפתחות שאי אפשר להבחין ביניהם; הזזה גורפת הייתה שוברת מחדש.
   **לבקש מהמורים לפתוח את הלוח ולשמור מחדש.**

3. **מחיקת חשבון דיווחה הצלחה בלי למחוק כמעט כלום.** `currentUser.delete()` רץ
   לפני ניקוי ה-DB, ולכן כל 14 המחיקות נכשלו ב-PERMISSION_DENIED ונבלעו ב-catch
   ריק. שרד גם `teacherVerification/<uid>` — תמונת ת"ז של קטין — שלא היה ברשימה
   כלל. **התיקון:** סדר הפוך + `getIdToken(true)` לשמירת הכוונה המקורית + ספירת
   כישלונות + 5 צמתים חסרים.
   ⬜ פער ידוע: `fcmTokens` ממופתח לפי טוקן וקריאתו admin-only — לקוח לא יכול
   לרשום את הטוקנים שלו ממכשירים אחרים. משוחרר רק המכשיר הנוכחי.

### מה שנשאר — לפי שערים (סדר מומלץ)

**שער 1 · שהאפליקציה תעבוד** (רובן מחיקות, ~יום)
- ⬜ למחוק את לולאת הסקר של 30 שניות (`index.html:13295`). כל 30 שנ' לכל משתמש
  לכל שיחה — **במקביל** למאזיני `child_added` שכבר מביאים אותן הודעות. 1.3TB/חודש
  ב-500 מורים מול מכסה של 10GB.
- ⬜ למחוק את מטפל ה-`fetch` ב-`sw.js:20-24`. נמדד: first-paint 340ms בלי SW מול
  13,044ms איתו — **פי 38**, על localhost בלי רשת. ה-fallback מת (caches נמחקים
  ב-install/activate) כך שאין offline בכלל.
- ⬜ `Cache-Control: no-store` → `no-cache` ב-`netlify.toml:24` **ולמחוק את `_headers`**
  (אותה מדיניות בשני מקומות — הפרת SSOT).
- ⬜ `_studentApprovalListenerUID` לא מתאפס ב-`firebaseLogout`. המאזין מנותק
  (13805) אבל השומר ב-12953 חוסם חיבור מחדש — **תלמיד שהתנתק והתחבר תקוע לנצח**.
  האינווריאנט בחוקה מתקיים באות ומופר במהות; `validate.sh` לא יכול לתפוס את זה.
- ⬜ מסך הבית מכריז "עוד אין מורים" ברשת איטית (`index.html:8737`) — התנאי בודק
  רק `TEACHERS.length===0` ולא את `teachersLoaded` שכבר קיים.
- ⬜ `loadRealTeachers()` מוריד את כל צומת `teachers` בכל התחברות (28.7MB ב-500).

**שער 2 · שילדים יהיו בטוחים** (~2-3 ימים) — **אסור לעבור בלעדיו**
- ⬜ קישור אישור חד-פעמי למייל ההורה שהוא-הוא מה שמשחרר את החשבון. היום פרטי
  ההורה מוקלדים על מכשיר הילד, בדיקת ת"ז היא ספרת ביקורת, ומה שפותח חשבון זה
  `verified` ידני. גם התיעוד היחיד שאפשר יהיה להציג בדיעבד.
- ⬜ `submitReport` קורא `db.ref('users').get()` — permission denied למי שאינו
  אדמין, נבלע ב-catch. **דיווחים לא מגיעים לאף אחד** והממשק מבטיח 24 שעות.
- ⬜ לקשור בין `ageVerification.status` ל-`teachers/<uid>/verified`. היום מנותקים
  לגמרי — מורה זמין לילדים בלי שאיש הסתכל על המסמך.
- ⬜ זיהוי טלפון/קישור/שם-משתמש בהודעות (לסמן, לא לחסום). היום אין כלום; הבדיקה
  היחידה היא 22 ביטויי גסות בצד לקוח, והודעות קוליות עוקפות אותה.

**שער 3 · שהחנות תאשר** (~יומיים + החלטה)
- ⬜ `privacy.html` מכחיש במפורש איסוף מסמכי זיהוי — בזמן שהאפליקציה אוספת ת"ז
  של קטינים. סתירה חזיתית מול הקוד.
- ⬜ הצהרת **Child Safety Standards / CSAE** — טופס חובה ב-Play שטרם מולא.
- ⬜ דף החנות מבטיח 3 מנגנוני בטיחות שלא קיימים בקוד — לבנות או להסיר.
- ⬜ מודרציית UGC (צ'אט, ביקורות, פוסטים, סטוריז, שו"ת) — הדרישה הרחוקה ביותר.
- **אפל:** שני חסמים שאינם באגים — **4.2** (העטיפה טוענת אתר חי) ו-**3.1.1**
  (מנוי מחוץ ל-IAP). **המלצה: לא לגשת לאפל עכשיו.** לסיים Google Play, לקבל
  משתמשים אמיתיים, ולחזור לאפל עם מוצר שיש לו מה להראות.

**שער 4 · שייראה טוב** (~יומיים, לא דחוף)
- ⬜ מצב לילה שבור: 193 מופעים inline של `color:#0F1A1F` על כרטיס `#161B22` =
  **1.02:1**, טקסט בלתי נראה. אין `prefers-color-scheme` בכלל.
- ⬜ כפתור ראשי: לבן על הגרדיאנט הטורקיז = **2.49:1** (נדרש 4.5:1), 129 מופעים.
  תיקון בטוח מותגית: `#0e7a72` שכבר קיים בגרדיאנט הכותרת המוגן.
- ⬜ טופס ארוך מתאפס באמצע הקלדה — 63 מתוך 111 שדות בלי `value=` binding.
- ⬜ 88 קריאות `icon()` לשמות שלא קיימים ב-`_SVGP` → מחרוזת ריקה. ב-9231 זה
  כפתור צבעוני ריק לגמרי.

**ניקיון — 20 דקות**
- ⬜ `game.html` (378KB, "משחק ידיעת הארץ") **חי כרגע בפרודקשן ב-`/game.html`**
  כי `publish = "."`. לא מקושר מ-Drushe. דף ציבורי לא-מתוחזק בדומיין של
  אפליקציית ילדים.
- ⬜ `Main.py` (בוט טלגרם לניטור מילות מפתח על איראן/חיזבאללה), `Procfile`,
  `requirements.txt`, ושני קבצים ששמם הוא שורת קוד פייתון שהודבקה לטרמינל.
  **נבדק: אין סודות מוטמעים** — הכל מ-`os.environ`.
- ⬜ `netlify.toml:1` כתוב `# Lamdeni` — שם אסור לפי החוקה. עובר בירוק כי
  `validate.sh:7` הוא `FILE="index.html"` — **הוולידטור סורק קובץ אחד בלבד.**
  להרחיב אותו לכל הקבצים ב-git (למעט `CLAUDE.md` ו-`validate.sh` עצמו).

### מה שדווקא תקין (נבדק ואומת)
- **ה-RTL מוקפד** — פסי ההדגשה, כפתורי החזרה והכפתור הצף כולם ממוקמים נכון.
  הבאגים האמיתיים מצומצמים: זנבות בועות הצ'אט, תג התראות אחד, ותצוגת טלפון.
- **כל האינדקסים ב-RTDB קיימים ונכונים** — אין ולו שאילתה אחת חסרת אינדקס.
- **אין סודות בריפו** מעבר למפתח ה-Web של Firebase, שהוא ציבורי בהגדרה.

## מה נעשה לאחרונה
- **דף המורה — ראש הדף וכפתור פעולה (24.8.2026, PR #104).** המשתמש ביקש
  עיצוב יפה יותר לדף המורה, קיבל שני מוקאפים חיים ובחר בכיוון "עומק
  המותג". שלוש תיקונים ממוקדים, שום דבר לא נמחק (וידאו/פורטפוליו/ביקורות/
  שו"ת/כפתורי שיתוף-הודעה-ביקורת/תהליך ההזמנה המקורי נשארו כמו שהיו):
  1. ראש הדף חזר לגרדיאנט המוגן (היה `${t.col}` של המורה, שבר SSOT מול
     כל שאר המסכים — למשל ראש מסך "קרוב אליי").
  2. רצועת "🌱 מורה חדש/ה" מוצגת מעל שלושת הסטטיסטיקות רק כשכולן אפס,
     במקום שלושה אפסים חשופים בלי הקשר.
  3. שורת פעולה קבועה בתחתית עם מחיר הניסיון וכפתור הזמנה — `position:fixed`
     כמו `.bnav` הקיים — כי כפתור ההזמנה המקורי היה בתחתית גלילה ארוכה.
  `APP_VERSION` עודכן ל-`4.11.1`.
- קפיצות בכניסה: איחוד רינדורים + דילוג על fire ראשון של מאזין userChats +
  המתנה למורים/סטוריז לפני הצגת הבית + עדכון תג ההודעות בלי רינדור מלא
  (`_refreshMsgBadge`). נשאר: לוודא במכשיר שאין יותר קפיצה.
- קהילה: אורח חסום מכתיבה/לייק/תגובה → מופנה להתחברות (`openCompose`).
- כותרות אודות/הודעות/קרוב-אליי: הוקטנו לגובה של דף הבית + תת-כותרות חדשות.
- דף מחיקת חשבון: `delete-account.html` (חי) — נדרש ל-Data Safety.
- חוקי Firebase (`database.rules.json`): פורסמו ידנית בקונסולה (אינדקסים + תיקון `!=true`).
- **תוקן ה-workflow "Deploy Firebase Rules"** — נכשל ב-19 ההרצות האחרונות.
  שורש הבעיה: `firebase deploy --only database,storage` בפקודה אחת. חוקי
  ה-database עברו ולידציה, ואז השלב של storage נפל ב-403 על
  `serviceusage.services.get` והפיל את כל הפקודה לפני השמירה. כלומר הרשאה
  חסרה ביעד אחד חסמה יעד אחר לגמרי — ולכן החוקים פורסמו ידנית חודשים.
  הפריסה פוצלה לשני שלבים, ו-database נפרס תמיד.
  ✅ חוקי ה-database נפרסים אוטומטית מאז 16.8.2026.

## ⚠️ מגבלת 4KB של Lambda — מה ששבר את הפריסה ליום שלם (23.8.2026)

**התסמין:** כל פריסה ל-Netlify נכשלה ב-
`Failed to create function: Your environment variables exceed the 4KB limit
imposed by AWS Lambda`. האתר החי נתקע על הגרסה מ-21.8, כולל בלי תיקוני
ה-XSS שמוזגו ב-#99.

**מה שנראה כמו הסיבה, ולא היה:** הוספת ארבעה משתני EmailJS (143 בתים).
מחקתי שלושה מהם, ואז את הרביעי — עד שהסביבה חזרה להיות **זהה בדיוק** למה
שעבד ב-21.8. זה עדיין נכשל. שם התברר שהחשבון בבתים היה התיאוריה הלא נכונה.

**השורש:** בהחלפת מפתח ה-Firebase הנחיתי למלא את
`FIREBASE_SERVICE_ACCOUNT_JSON` בשלושה הקשרי פריסה נפרדים, במקום לבחור
"Same value for all deploy contexts". קודם הוא היה ערך אחד עם
`context: all`. אחרי זה — **חמישה עותקים** של 2,375 בתים, כלומר ~11,900
מול תקציב של 4,096. לכן קיצוץ של 143 בתים לא יכול היה לעזור: הפער היה פי
שלושה, לא עשרות בתים.

**מה שגם התברר בדרך, וחשוב לדעת מראש:**
- Netlify **מכריחה** משתנה המסומן `Contains secret values` להתפצל לערך
  נפרד בכל הקשר פריסה. עבור ערך גדול זה מכפיל אותו פי חמישה בתוך התקציב.
- **אי אפשר לבטל** סימון `secret` על משתנה קיים — התיבה נעולה. הדרך היחידה
  היא למחוק את המשתנה וליצור אותו מחדש.
- `Lambda compatibility mode`, שהודעת השגיאה מציעה לעזוב כדי לבטל את
  המגבלה, **לא מופיע** במסך Functions בתוכנית הזו. זו לא אפשרות.

**הכלל להמשך:** ערך גדול — ובעיקר קובץ חשבון שירות — נשמר תמיד כ-**ערך
אחד** עם "Same value for all deploy contexts", ולא מסומן כסודי.

**ולמה לא מסומן כסודי:** כתיבת משתנה עם `context: all` **ו**-`secret: true`
דרך ה-API **מחזירה "upserted" ולא שומרת כלום**. אין שגיאה ואין אזהרה —
המשתנה פשוט אינו קיים אחר כך. השילוב הזה הוא בדיוק מה שנחסם: סודי מחייב
ערך נפרד בכל הקשר, וזה מה שיצר את חמשת העותקים מלכתחילה.

**לאמת אחרי כל כתיבה של משתנה סביבה. לא להסתמך על תשובת ההצלחה.** כאן זה
התגלה רק אחרי שהמשתנה הישן כבר נמחק, והאישור נעלם מהאתר לכמה דקות.

**המבנה שנקבע בסוף:** מתוך 2,375 הבתים של קובץ השירות רק המפתח הפרטי הוא
סוד. `netlify/lib/firebase-admin-init.js` מרכיב את האישור מ-`project_id`
ו-`client_email` המוטבעים בקוד (שניהם ציבוריים ממילא) ומ-
`FIREBASE_PRIVATE_KEY` בלבד. הקריאה של ה-JSON המלא נשמרת כנפילה לאחור.

**מצב הסביבה כרגע:** `FIREBASE_PRIVATE_KEY` (ערך אחד, `context: all`,
1,732 בתים) ו-`ANTHROPIC_API_KEY`. `FIREBASE_SERVICE_ACCOUNT_JSON` נמחק.

⬜ **חוב פתוח 1:** `FIREBASE_PRIVATE_KEY` אינו מסומן כסודי, מהסיבה שלמעלה,
ולכן הוא קריא בטקסט גלוי דרך ה-API — בדיוק הבעיה שבגללה הוחלף המפתח ב-22.8.
זה מצב ביניים שנבחר בכוונה: אתר שאינו מתעדכן כלל היה גרוע יותר. שים לב
שסימון "secret" בממשק יפצל אותו לחמישה הקשרים, 5 × 1,732 = 8,660, וזה שוב
חורג מ-4KB. כלומר אם רוצים אותו סודי, למלא **רק** את production ולהשאיר את
שאר ההקשרים ריקים.

⬜ **חוב פתוח 2:** המפתח הנוכחי עבר בתמלול השיחה בזמן האבחון, כי המשתנה היה
קריא. כשיהיה זמן — ליצור מפתח חדש ב-Firebase Console, להזין רק אותו, ולמחוק
את הישן ב-Google Cloud Console. אותו תהליך שכבר בוצע ב-22.8.

⬜ **חוב פתוח 3:** `EMAILJS_PRIVATE_KEY` נמחק במהלך האבחון וצריך להיות מוזן
מחדש (dashboard.emailjs.com → Account → General → Private Key). בלעדיו
איפוס הסיסמה נשען על המפתח הציבורי בלבד.

## 🔒 מפתח ה-admin של Firebase — הוחלף (22.8.2026)

התגלה תוך כדי הגדרת משתני EmailJS בנטליפיי, כי אז נקראה לראשונה רשימת
המשתנים במקום להניח מה יש בה.

**מה היה:** `FIREBASE_SERVICE_ACCOUNT_JSON` היה מוגדר **לא-סודי**
(`is_secret: false`). המשמעות היא שהמפתח הפרטי המלא הוחזר בטקסט גלוי בכל
קריאה ל-API של נטליפיי ובממשק — לא מוסתר, לא מוגן. המפתח נותן גישת אדמין
מלאה ל-RTDB, כולל תעודות הזהות של הקטינים, ועוקף את כל חוקי הגישה.

**ובנוסף:** בחשבון השירות היו **חמישה** מפתחות פעילים. כל לחיצה היסטורית
על "Generate new private key" יצרה עוד אחד, ואיש לא מחק אף אחד. ארבעה מהם
לא היו בשימוש בשום מקום — וכל אחד מהם היה גישת אדמין חיה.

**מה נעשה:** נוצר מפתח חדש, הוגדר בנטליפיי עם `Contains secret values`
בסקופ `builds + functions + runtime`, וכל חמשת הישנים נמחקו ב-Google Cloud
Console.

**איך זה אומת, וזה החלק שחשוב לשחזר:** העובדה שאיפוס סיסמה עבד אחרי
ההחלפה **לא** הוכיחה שהמפתח החדש בשימוש — הישן עדיין היה פעיל, ולכן גם הוא
היה עובר את אותה בדיקה. רק מחיקת הישן ובדיקה חוזרת מפרידה בין השניים. אותה
שיטה חלה על כל החלפת סוד: להחליף, למחוק את הישן, ורק אז לבדוק.

**כלל להמשך:** כל סוד חדש בנטליפיי נוצר עם `Contains secret values`
מסומן. נטליפיי תוריד אוטומטית `All scopes` ל-Specific בלי `Post processing`
— זה תקין ואין מה לתקן. אחרי כל יצירת מפתח שירות — למחוק את הקודם באותו
יום, לא "אחר כך".

## 🔒 חשיפת תמונות ת"ז של קטינים — תוקן (18.8.2026)
**הממצא החמור ביותר בסשן.** התגלה תוך כדי מרדף אחרי פריסת חוקי storage.

**שרשרת הסיבות:**
1. Firebase Storage מעולם לא הוגדר בפרויקט (דורש תוכנית Blaze בתשלום)
2. לכן **כל** קריאה ל-`uploadBase64ToStorage` נכשלה
3. לפונקציה יש נפילה שקטה לאחור שמחזירה את ה-base64 המקורי
4. הערך נשמר תחת `teachers/<uid>/ageVerification/idPhoto`
5. ול-`teachers` יש `".read": "auth != null"`

**התוצאה:** כל משתמש מחובר — כולל תלמיד וכולל אורח אנונימי — יכול היה לקרוא
את תעודות הזהות של כל המורים הקטינים. וגרוע מכך: `loadRealTeachers()` מוריד
את כל צומת `teachers` **בכל כניסה**, כך שהתמונות הגיעו למכשיר של כל משתמש
בלי שאיש ביקש.

**למה זה חמק:** הכל נראה תקין — התמונות מוצגות בפאנל, ההרשמה עובדת. הנפילה
לאחור עשתה בדיוק את מה שנועדה לעשות, אבל העבירה את המסמך הרגיש ביותר
באפליקציה למקום הפתוח ביותר.

**התיקון:** התמונה עברה ל-`teacherVerification/<uid>/idPhoto`, שקריא רק
לבעלים (חוק ייעודי) ולמנהל (חוק השורש). שאר שדות `ageVerification` (גיל,
סוג מסמך, סטטוס) נשארו ב-`teachers/` — הם לא המסמך, ומשמשים לתג "גיל מאומת".

⚠️ **הערה על חוקי RTDB:** אי אפשר לשלול הרשאת קריאה בצומת-בן אחרי שההורה
נתן אותה. לכן חובה היה **להזיז את הנתון** — הוספת חוק מגביל על
`ageVerification` לא הייתה עושה כלום.

⬜ **פעולה נדרשת ממך:** בפאנל הניהול → לשונית מורים מופיע כרטיס אדום
"🔒 N תמונות מסמך במקום חשוף" עם כפתור העברה. **ללחוץ פעם אחת.** הוא כותב
ליעד לפני שהוא מוחק מהמקור, בטוח להרצה חוזרת, ונעלם מעצמו כשאין מה להעביר.

💡 **Firebase Storage** עדיין לא מוגדר ודורש Blaze. זה בסדר — התמונות
נשמרות ב-RTDB בצומת מוגן וזה תקין בהיקף הנוכחי. אם תשדרג בעתיד, ההעלאות
ילכו ל-Storage מעצמן ו-`storage.rules` (שכבר קיים) יגן עליהן.

### פריסת חוקי storage — נעצר במכוון (18.8.2026)
שלושה חסמים נפתרו בזה אחר זה, וכל אחד קידם את השגיאה לשלב הבא:
1. ✅ הוענק `roles/serviceusage.serviceUsageConsumer` → בדיקת ה-API נפתחה
2. ✅ הופעל `firebasestorage.googleapis.com` בקונסולה → הבדיקה ירוקה
3. ✅ הוענק **Cloud Storage for Firebase Admin** → ועדיין:

```
403 Permission 'firebasestorage.defaultBucket.get' denied
on '//firebasestorage.googleapis.com/projects/kidemy-83a17/defaultBucket'
(or it may not exist)
```

**ההחלטה: שלב ה-storage הפך לאזהרה במקום כישלון**, ולא ממשיכים לרדוף אחריו.
הנימוק: `storage.rules` לא נגע מאז יצירת הריפו, בעוד חוקי ה-database משתנים
בפועל ונפרסים כבר. job שנשאר אדום לתמיד מאמן את כולם להתעלם מהכישלון הבא —
וזה בדיוק מה שקרה כאן ב-19 ההרצות הראשונות.

⬜ **אם משנים את `storage.rules`** — לפרסם ידנית:
https://console.firebase.google.com/project/kidemy-83a17/storage/rules
זה הקובץ שמגן על תמונות הת"ז של הקטינים, אז אסור להניח שהשינוי עלה.

💡 שני כיווני חקירה אם חוזרים לזה: (א) ייתכן שהאיחור בהתפשטות IAM הוא
האשם — שווה הרצה חוזרת אחרי כמה דקות; (ב) ההודעה אומרת "or it may not
exist", וייתכן שהדלי לא רשום כ-defaultBucket ב-API הזה למרות שהוא קיים
ועובד (העלאות תמונות ת"ז עובדות בפועל — אומת מול המשתמש).

## אנליטיקס והסכמת הורים (17.8.2026)
מדיניות הפרטיות הצהירה שאין מעקב מעבר ל-localStorage, בעוד `index.html`
קרא ל-`firebase.analytics()` כבר באתחול — טוען gtag.js, מציב עוגיית `_ga`
ושולח `screen_view` ממסך הפתיחה, לפני שמישהו התחבר ובוודאי לפני אישור הורה.
באפליקציה שהוצהרה כמיועדת לילדים, ושגוגל מצליבה את Data Safety שלה מול
התנהגות ה-SDK בפועל, זה פער שמסכן השעיה ולא רק דחייה.

**הפתרון:** לא לאתחל אנליטיקס בכלל עד שיש הסכמה.
`setAnalyticsCollectionEnabled(false)` אחרי האתחול מאוחר מדי — gtag כבר
נטען והעוגייה כבר הוצבה. לכן `initAnalyticsIfConsented()` דוחה את הקריאה
ל-`firebase.analytics()` עצמה. השער: `currentUserData.verified === true` —
הדגל שכבר נקבע אחרי אישור הורה+צוות, ולא מנגנון הסכמה מקביל חדש.
ביציאה `stopAnalyticsCollection()` עוצר איסוף.

הגיבוי ב-`_analytics/<חודש>` ב-RTDB ממשיך לעבוד — זה מידע first-party
בשרת שלנו, לא תלוי בגוגל.

`privacy.html` עודכן לתאר בדיוק את זה (סעיף 8) + תאריך אוגוסט 2026.

⬜ **נשאר למשתמש:** לוודא ב-Play Console ששאלון Data Safety מצהיר
`App activity → App interactions` למטרת **Analytics**. עכשיו כשהמדיניות
מצהירה על מדידה, ההצהרה חייבת לומר אותו דבר.

## אפל — עדכון 2: להתקדם כמה שאפשר, חוץ מלחיצת Submit (24.8.2026)
המשתמש מדווח שקשה לגייס 12 בודקי אנדרואיד כי רוב הישראלים עם אייפון (היה 9
מתוך 12; מצפה לסגור את ה-12 השבוע). **ביקש במפורש להתקדם כמה שאפשר לקראת
הגשה לאפל במקביל**, לא רק רישום — זו הרחבה של העדכון הראשון למטה.

**מה שמותר להתקדם בו עכשיו, לפי הסדר:**
1. סיום רישום Apple Developer Program (אם עוד לא הושלם).
2. App Store Connect: יצירת App ID תואם ל-`app.drushe.mobile`, ורישום
   האפליקציה (New App) + מילוי מטא-דאטה מוקדם (הרבה ניתן להעתיק מ-Google
   Play listing שכבר קיים).
3. תעודת חתימה (Distribution Certificate) + Provisioning Profile — אפשר
   בלי מק, CSR ניתן לייצר גם ב-OpenSSL.
4. הרחבת `build-ios.yml` ל-workflow חתום שמעלה ל-TestFlight (macOS runner
   ב-GitHub Actions, אותו דפוס בדיוק כמו secrets ה-Android הקיימים) —
   זו עבודת קוד; דורשת שהמשתמש יעלה את התעודה/הפרופיל/מפתח ה-API כ-secrets
   קודם.
5. TestFlight — המקבילה של Closed testing בגוגל.

**⚠️ מה שעדיין לא מומלץ, ונדבר עליו כשמגיעים לשלב:** ללחוץ בפועל
"Submit for Review". ה-4.2 (מעטפת Capacitor שטוענת אתר חי) וה-3.1.1
(IAP) לא טופלו בקוד, ודחייה כמעט ודאית שורפת מחזור בדיקה (ימים) לחינם.
עד שנדבר על זה — כל השלבים 1-5 למעלה בטוחים להתקדם בהם.

**⚠️ ההחלטה המקורית למטה (לא להגיש בפועל) לא בוטלה, רק נדחתה לשיחה
נקודתית בשלב ה-Submit.** אל תניחו שהיא בטלה לגמרי בלי לחזור לזה.

## אפל — הוחלט: לא עכשיו (17.8.2026)
מתמקדים בגוגל. הסיבות, לתיעוד כדי שלא נדון בזה מחדש:
- שעון 14 היום של Closed testing הוא הנתיב הקריטי; פיצול תשומת לב מעכב אותו.
- **Guideline 4.2** של אפל (מקבילה ל-Minimum Functionality) נאכפת הרבה יותר
  בתקיפות מזו של גוגל. מעטפת Capacitor שטוענת אתר היא דחייה קלאסית שם.
- **Guideline 3.1.1** — מנוי דיגיטלי כמעט תמיד ייאלץ לעבור דרך IAP של אפל
  (15-30% עמלה). אצל גוגל יש יותר מרחב טיעון.
- אין מק ואין אייפון למשתמש — הגשה בעיוורון.
- עלות: $99 **לשנה** מתחדש, מול $25 חד-פעמי בגוגל.

`build-ios.yml` כבר בונה ל-סימולטור בלי חתימה, כלומר הפרויקט מתקמפל.
כשמתקדמים יחסרו רק: חשבון בתשלום, תעודת חתימה ו-provisioning profile.

**טריגר לחזור לזה:** כששניים מהשלושה מתקיימים — חי בפרודקשן בגוגל אחרי
בדיקה · יש מורים ותלמידים אמיתיים · הוכרעה שאלת ה-IAP.

## Google Play — סטטוס פרסום (המשימה הפעילה)
מיקום: מרכז שליטה של האפליקציה ב-Play Console.
- ✅ מדיניות פרטיות: `https://kidemy-app.netlify.app/privacy.html`
- ✅ פרטי כניסה (App access): חשבון בדיקה — שם משתמש `googlereview`, סיסמה `Drushe-Review-2026`
  (חשבון תלמיד; המשתמש יצר אותו). הוראה לבודק: "מזינים שם משתמש בלבד, ללא מייל".
- ✅ אבטחת נתונים (Data Safety): הושלם ונשמר. כתובת מחיקה = delete-account.html.
- ✅ קהל יעד: 9–12, 13–15, 16–17 · "מיועד לילדים = כן" (חלה מדיניות Families).
- ✅ קטגוריה: חינוך · קשר: rotemshaer@gmail.com · אתר: kidemy-app.netlify.app · תגים: מדריך לימוד + לימוד שפות.
- ✅ דף החנות (Store listing): הועלה במלואו — טקסטים + כל הגרפיקה. הדף מסומן
  ב-Play Console כ**"מוכנה לשליחה לבדיקה"**.
  מקור הטקסטים: `mobile/store-listing.txt`. מקור הגרפיקה: `mobile/store-assets/`
  (אייקון 512, feature-graphic 1024x500, 3 צילומי טלפון 1080x1920,
  3 צילומי טאבלט 1600x2560 שתקפים גם ל-7" וגם ל-10"). וידאו — לא נדרש.
  עמוד להורדת כל התמונות בנייד: https://claude.ai/code/artifact/413ac77e-7c9a-4c52-a5cf-ac335a94116c
- ✅ מודעות = לא · ממשלתי = לא · תכונות פיננסיות = לא · Advertising ID — הוצהרו.
- ✅ **סיווג תוכן (IARC)** — הוגש 16.8.2026, סטטוס "הושלמה".
  התשובות שנבחרו: קטגוריה "כל סוגי האפליקציות האחרים" (לא "חברתית" — המטרה
  העיקרית היא הזמנת שיעורים, לא היכרות); תוכן פוגעני בחבילה=לא; שיתוף תוכן
  משתמש=כן; UGC מקור ראשי=לא; עירום/אלימות=לא; חסימה=כן; דיווח=כן;
  בקרת צ'אט=כן; מוגבל לחברים מוזמנים=לא; תוכן מקוון=כן; שיתוף מיקום מדויק=לא
  (ה-GPS מעוגל ל-2 ספרות ומשמש רק לחישוב מרחק); רכישות דיגיטליות=לא;
  תגמולי מזומן/קריפטו=לא; דפדפן/מנוע חיפוש=לא; חינוך=כן.
- ✅ **הצהרת אפליקציות בריאות** — הוגשה ("אין תכונות בנושא בריאות").
  מסך "תוכן האפליקציה" מציג כעת: "אין עוד הצהרות שממתינות לטיפול".

## המשימה הפעילה: להשלים את מסלול הבדיקה הפנימית
מרכז השליטה מציג צ'קליסט "1 מתוך 3 הושלמו":
1. ⬜ **בחירת בודקים** — צריך רשימת מיילים. המשתמש אוסף 12+ כתובות.
   הקובץ `mobile/testers.csv` (מייל אחד בכל שורה, בלי כותרת) — **ב-gitignore**,
   כי הריפו ציבורי ואסור לפרסם מיילים של אנשים.
   דרישות: כל מייל חייב להיות חשבון Google; כל בודק חייב לאשר את ההזמנה.
   ⚠️ **וגם מכשיר אנדרואיד.** בעל אייפון יכול לאשר הזמנה אבל לא יכול להתקין,
   וגוגל בוחנת מעורבות אמיתית ולא רק מי לחץ אישור — אז הוא לא נספר בפועל.
   בעת האיסוף לשאול במפורש "יש לך טלפון אנדרואיד?".
2. ✅ יצירת גרסה חדשה.
3. ⬜ **תצוגה מקדימה ואישור של הגרסה** — משחרר בפועל, ואז נפתח גם
   "שליחת האפליקציה לבדיקה".

⚠️ **נקודה קריטית ללוח הזמנים:** בדיקה פנימית (Internal testing) **לא** נספרת
לדרישת גוגל לחשבון מפתח אישי חדש — 12+ בודקים ב-**Closed testing** במשך
**14 יום רצופים** לפני Production. זה שעון קלנדרי, לא עבודה טכנית, אז כדאי
להתחיל אותו מוקדם ככל האפשר. אפשר לחבר את זה למגייסי הקמפיין.

## התראות נייטיב (Push / FCM) — נבנה, ממתין ל-google-services.json
**למה זה נעשה:** במניפסט הייתה הרשאה אחת (INTERNET) ואפס פלאגינים נייטיב,
כלומר האפליקציה הייתה webview טהור של האתר. זו הקטגוריה שגוגל דוחה תחת
מדיניות **Minimum Functionality**. התראות Push הן פונקציונליות נייטיב אמיתית
שאתר לא יכול לספק, ולכן זה גם מקטין את סיכון הדחייה וגם משפר את המוצר.

**מה כבר בקוד (צד הקבלה):**
- `@capacitor/push-notifications` ב-`mobile/package.json` (+ lockfile).
- ב-`index.html`: `_registerNativePush()` מבקש הרשאה, נרשם, ושומר את הטוקן;
  מטפל בהודעה שמגיעה בחזית ובהקשה על התראה (פותח צ'אט או מסך התראות).
  נקרא מ-`onAuthStateChanged` אחרי שיש משתמש מחובר. בדפדפן יוצא מיד.
- **מבנה הנתונים:** `fcmTokens/<token> = {uid, updatedAt}` — לפי טוקן, לא לפי
  uid. הטוקן שייך למכשיר: כך החלפת חשבון על אותו מכשיר דורסת את הבעלים
  אוטומטית, וריבוי מכשירים עובד מעצמו. מפתוח לפי uid היה משאיר טוקן תלוי
  אצל המשתמש הקודם וממשיך לשלוח לו התראות (עם תצוגה מקדימה של הודעות)
  למכשיר של מישהו אחר.
- `auth.signOut` עטוף פעם אחת כדי לנתק את הטוקן **לפני** היציאה (אחריה חוקי
  ה-DB כבר דוחים את המחיקה). אין בקוד פונקציית logout אחת — היציאה נקראת
  ישירות מ-onclick בעשרות מקומות — ולכן עטיפה היא נקודת החיבור היחידה.
- חוק חדש ב-`database.rules.json` תחת `fcmTokens` — נפרס אוטומטית במיזוג ל-main
  (ראה למטה; ה-workflow תוקן ולא צריך יותר פרסום ידני).
- `scripts/write-google-services.sh` + שלב בשני ה-jobs ב-`build-android.yml`.

**⬜ מה חסר כדי שזה יעבוד — רק המשתמש יכול לספק:**
1. Firebase Console → פרויקט `kidemy-83a17` → הוסף אפליקציית **Android** →
   package name **`app.drushe.mobile`** (חייב להיות מדויק) → הורד
   `google-services.json`.
2. GitHub → Settings → Secrets → Actions → סוד חדש **`GOOGLE_SERVICES_JSON`**
   עם כל תוכן הקובץ. הקובץ עצמו ב-gitignore (הריפו ציבורי).
   בלי הסוד הבנייה עדיין מצליחה — רק בלי התראות, עם warning.
3. Netlify → Site settings → Environment variables → `FIREBASE_SERVICE_ACCOUNT_JSON`
   (אותו ערך שכבר קיים כ-secret ב-GitHub) — בשביל צד השליחה למטה.

חוקי ה-DB כבר לא דורשים פרסום ידני — הם נפרסים אוטומטית במיזוג ל-`main`.

**✅ צד השליחה — נבנה כ-Netlify Function (מיידי, חינם, בלי Blaze):**
`netlify/functions/send-push.js`, חשוף כ-`/api/send-push` (ראה `netlify.toml`).
נקרא מ-`_sendPushToUser()` אחרי שליחת הודעת טקסט והודעה קולית.

עקרון האבטחה: **השרת לא מאמין ללקוח**. הלקוח שולח רק `{idToken, toUid, type}`
— לא את זהותו ולא את טקסט ההתראה. השרת:
1. מאמת את ה-idToken ומכאן יודע מי השולח (לא ממה שנטען).
2. קורא את ההודעה האחרונה ב-`messages/<chatId>`, ומוודא שהיא מהשולח הזה
   ושנכתבה ב-60 השניות האחרונות — כלומר שההודעה באמת נשלחה עכשיו, ולא
   שיגור חוזר של התראה על הודעה ישנה כדי להציק.
3. קורא את שם השולח מ-`users/<uid>/name` ובונה את הטקסט בעצמו.
4. תקרת קצב של 40 התראות לדקה לשולח (`pushRateLimit/<uid>`, transaction).
5. מנקה טוקנים מתים לפי קוד השגיאה של FCM.
בלי זה כל משתמש מחובר היה יכול לשגר לכל אחד אחר התראה עם תוכן שרירותי.

⬜ **נדרש ממך:** Netlify → Site settings → Environment variables → להוסיף
`FIREBASE_SERVICE_ACCOUNT_JSON` (אותו ערך שכבר קיים כ-secret ב-GitHub).
בלעדיו הפונקציה מחזירה 500 והצ'אט ממשיך לעבוד רגיל — רק בלי Push.

⬜ סוגי התראה נוספים (`newBooking` וכו') עדיין לא נתמכים. הרשימה סגורה
בכוונה: הוספת סוג מחייבת לכתוב לו אימות משלו, ולא תיכנס בטעות בלעדיו.

**✅ אומת בבנייה אמיתית** (ריצה 31969796355, 16.8.2026) — הלוגים מראים:
- `Found 1 Capacitor plugin for android: @capacitor/push-notifications@8.1.2`
- `Wrote mobile/android/app/google-services.json (1069 bytes)` — כלומר הסוד
  נקרא, ולא מסלול האזהרה של "סוד חסר".
- `> Task :app:processDebugGoogleServices` — ההוכחה המכרעת: תוסף
  google-services הוחל בפועל. הוא מוחל רק אם הקובץ קיים.
- `:capacitor-push-notifications:compileDebugJavaWithJavac` → `BUILD SUCCESSFUL`

**⚠️ מה עדיין לא נבדק: התנהגות על מכשיר אמיתי.** שהבנייה מצליחה לא מוכיח
שההתראה מגיעה. כדי לבדוק באמת צריך: למזג ל-main (כדי שהפונקציה תתפרסם
ל-Netlify), להתקין את ה-APK, ולשלוח הודעה בין שני חשבונות.

## בניית ה-AAB
**הערה חשובה: לרוב אין צורך לבנות מחדש.** ב-`mobile/capacitor.config.json`
מוגדר `server.url = https://kidemy-app.netlify.app`, כלומר האפליקציה היא מעטפת
שטוענת את האתר החי בזמן ריצה. כל שינוי ב-`index.html` שמתפרסם ל-Netlify מגיע
למשתמשים אוטומטית **בלי** AAB חדש. בונים מחדש רק כשמשתנה משהו נייטיב
(אייקון, הרשאות, targetSdk, פלאגין).

1. GitHub → Settings → Secrets → Actions → 4 secrets מהקובץ ששלחתי למשתמש
   (`drushe-android-secrets.txt`): ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD,
   ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD. **הסודות אינם בריפו** — רק אצל המשתמש.
2. GitHub → Actions → "Build Android App" → Run workflow (main) → הורד artifact `drushe-release-aab`.
3. העלה `app-release.aab` ל-Play Console.

`versionCode` מוזרק אוטומטית מ-`github.run_number` (ראה `build-android.yml`
ו-`mobile/android/app/build.gradle`), כי Play דוחה AAB עם versionCode שכבר
שימש. אין צורך לערוך אותו ידנית לפני כל העלאה.

## קמפיין גיוס מורים (אורגני, בלי תקציב)
נמסר בצ'אט (לא artifact — המשתמש ביקש בלי artifact). עיקר: טיקטוק (נפח + hooks) +
השתלה בקבוצות ווטסאפ + שגרירים/מיקרו-משפיענים. יש 15 רעיונות לסרטונים ו-2 הודעות פנייה.

## ⚠️ דגל שצריך לכבות ידנית: PLATFORM_HAS_STUDENTS
ב-`index.html` יש `const PLATFORM_HAS_STUDENTS=false`. כל עוד הוא `false`,
מסך הרשמת המורה מציג כרטיס "בגילוי מלא — עדיין אין תלמידים בפלטפורמה".

**ביום שיש תלמידים אמיתיים — להחליף ל-`true`.** אחרת הטקסט הופך לשקר
שמרחיק מורים בדיוק כשכבר יש להם למי ללמד.

הדגל ידני בכוונה ולא נגזר אוטומטית: חוקי ה-DB לא מאפשרים ללקוח לספור
משתמשים, וניחוש היה משקר בשקט לאחד משני הכיוונים.

## פתוח / לבדוק
- **לאסוף 12+ מיילים של בודקים** ולהשלים את הצ'קליסט למעלה (המשימה הפעילה).
- לוודא במכשיר שקפיצות הכניסה נעלמו (גרסה 4.9.87).
- ✅ **כתובת מדיניות הפרטיות ב-Play Console — אומתה תקינה** (17.8.2026).
  מה שנראה כמו `kidemy-fy.app` בצילום מסך היה גלישת שורה של
  `kidemy-app.netli**fy.app**` במסך צר, לא כתובת שגויה. אומת בהעתקה
  מהשדה והדבקה בדפדפן — הדף נטען. **לא לפתוח את זה שוב.**
- ✅ **Data Safety — הוצהר על אנליטיקס** (17.8.2026): "אינטראקציות
  באפליקציה" → נאספים=כן, משותפים=לא, מטרה=ניתוח נתונים.
  ⚠️ הקטגוריה "מזהה מכשיר" (לטוקן FCM) **לא הופיעה** בטופס — ייתכן שגוגל
  מסתירה אותה לאפליקציות שהוצהרו כמיועדות לילדים. במקום זאת התיעוד נמצא
  ב-`privacy.html` (סעיף מזהה התראות). לבדוק שוב אם גוגל תעיר על זה.
- לוודא במכשיר שהתראות Push מגיעות בפועל (שני חשבונות, אפליקציה סגורה).
- פריסת חוקי storage — נעצרה במכוון, ראה הסעיף למעלה. לא לפתוח מחדש בלי סיבה.
- 2 חולשות אבטחה שדווחו ב-`npm ci` של `mobile/` — לבדוק אם מגיעות לקוד שרץ.

## שאלה פתוחה: לבטל את מנוי המורים? (נשאלה 17.8.2026)
המשתמש שוקל להפוך הכל לחינם ולהרוויח בהמשך. הרקע והשיקולים תועדו כדי
שההחלטה לא תידון מאפס:
- **בעד ביטול:** שאלת ה-IAP (סעיף 3 ב-`age-rating-answers.txt`) נעלמת
  לחלוטין; מרקטפלייס בלי מורים חסר ערך, ו-₪79 לפני שיש תלמידים הוא חסם
  גיוס אמיתי; מעבר חינם→בתשלום בהמשך קל בהרבה מבתשלום→חינם→בתשלום.
- **נגד:** התשלום הוא גם מסנן איכות. באפליקציה שילדים משתמשים בה, מורה
  שלא השקיע כלום נושר יותר בקלות — וזו עלות בטיחותית, לא רק עסקית.
- ⚠️ **מונטיזציה עתידית — שתי מגבלות קשיחות:**
  1. **מכירת/שיתוף דאטה אינה אופציה.** האפליקציה הוצהרה כמיועדת לילדים
     (Families policy אוסרת), `privacy.html` מבטיח מפורשות "לא מוכרים מידע
     לצד שלישי לעולם", וזה סותר את הבטחת הליבה של המוצר להורים.
     שימוש בנתונים אגרגטיביים לשיפור המוצר עצמו — מותר ותקין.
  2. **קידום בתשלום בחיפוש** אפשרי, אבל חייב להיות מסומן כ"מקודם", וכשמורה
     משלם דרך האפליקציה זה **מחזיר את שאלת ה-IAP** — לא פותר אותה.
- `COMMISSION_RATE=0` הוא כלל בלתי משתנה ב-CLAUDE.md, כך שעמלה על שיעורים
  אינה על השולחן אלא אם המשתמש משנה את החוקה במפורש.
