# Drushe — מסמך רקע לביקורת הנדסית

מסמך לקורא חיצוני שרואה את הפרויקט בפעם הראשונה. המטרה שלו היא שתוכל לשפוט
את המערכת, לא שתתרשם ממנה — ולכן החולשות כאן מפורטות באותה רמת פירוט כמו
מה שעובד.

עודכן: 6.9.2026 · `APP_VERSION` 4.11.16 · `main` על `488522c`

---

## 1. מה זה

פלטפורמת שיעורים פרטיים שבה **בני נוער (14–18) מלמדים ילדים צעירים מהם**,
בפיקוח הורים. **כל המשתמשים בפלטפורמה הם קטינים.** זו העובדה שקובעת כמעט כל
החלטה ארכיטקטונית כאן — היא הסיבה שיש אישור הורה בשרת, אימות גיל מול מסמך,
וסריקת הודעות.

**מצב עסקי:** טרם עלה לאוויר. ממתין לאישור גוגל ל-Closed Testing עם 13
בודקים. אפס משתמשים אמיתיים (2,548 בקשות web בחודש = בעלים שבודק).

**מודל:** 500 המורים הראשונים חינם לתמיד, אחר כך ₪49/חודש למורה.
`COMMISSION_RATE=0` — אין עמלה על שיעורים, התשלום תלמיד↔מורה מחוץ למערכת.

---

## 2. ארכיטקטורה

```
Android APK (Capacitor)  ──טוען בזמן ריצה──▶  kidemy-app.netlify.app
                                                      │
                                          ┌───────────┴───────────┐
                                          │                       │
                                   index.html            Netlify Functions
                              (14,435 שורות, קובץ יחיד)    (4 פונקציות, Node)
                                          │                       │
                                          └───────────┬───────────┘
                                                      ▼
                                          Firebase RTDB (50 צמתים)
                                            + Firebase Auth
```

| רכיב | פירוט |
|---|---|
| Frontend | **קובץ HTML יחיד**, 14,435 שורות, 1.1MB (255KB דחוס). ללא build step, ללא framework, ללא bundler. |
| ספריות | Firebase JS SDK מ-gstatic, ועוד אחת מ-jsDelivr. זהו. |
| Backend | 4 Netlify Functions: `parent-consent`, `password-reset`, `send-push`, `ai-assistant` |
| DB | Firebase Realtime Database. אבטחה **כולה** ב-`database.rules.json`. |
| Mobile | Capacitor. `server.url` מצביע לאתר החי — **האפליקציה היא מעטפת, לא עותק.** |
| CI | 6 workflows ב-GitHub Actions |

### מה שחשוב להבין לפני שמבקרים

**אין שרת אפליקציה.** הלקוח מדבר ישירות מול RTDB. ארבע הפונקציות קיימות רק
למה שהלקוח **אסור** שיעשה: לאשר הורה, לאפס סיסמה, לשלוח Push, ולקרוא ל-LLM.
המשמעות: **חוקי ה-DB הם שכבת האבטחה היחידה.** באג בחוק הוא באג בייצור.

**האפליקציה בחנות טוענת את האתר החי.** אין AAB חדש לכל שינוי — פריסה ל-Netlify
מגיעה למשתמשים מיד. הצד השני: **Netlify מושעית = מסך לבן לכל המשתמשים.**

---

## 3. שלוש החלטות שנראות מוזרות ויש להן סיבה

**קובץ HTML אחד של 14 אלף שורות.** נבנה ע"י בעלים לא-מתכנת בעזרת LLM,
מהטלפון בלבד. אין לו מחשב ואין טרמינל. build step היה הופך כל שינוי לבלתי
אפשרי עבורו. זה מחיר מודע: אין tree-shaking, אין code splitting, וכל CSP
ריאלי יידרש ל-`unsafe-inline` (ולכן אין CSP — ראה §6).

**כל החשבונות נוצרים עם כתובת מייל מומצאת** `<username>@kidemy.app`.
קטין לא נדרש למייל אמיתי. תופעת הלוואי: `sendPasswordResetEmail` **מצליח**
ושולח לשומקום — מה שהצריך פונקציית שרת ייעודית שמאתרת את החשבון ושולחת
ל-`parentEmail` של ההורה.

**`verified` ניתן להצבה ל-true רק ע"י המנהל.** ילד לא יכול לשחרר את עצמו.
ההורה, שאינו מחובר לאפליקציה, גם הוא לא — ולכן האישור עובר דרך
`parent-consent.js` עם admin SDK שעוקף חוקים.

---

## 4. מודל הנתונים

50 צמתים בשורש. הדפוס הדומיננטי הוא **שכפול לאינדקסים** — למשל הזמנה נכתבת
לשלושה מקומות:

```
bookings/<id>                      ← הרשומה הגלובלית
userBookings/<studentId>/<id>      ← אינדקס התלמיד
teacherBookings/<teacherId>/<id>   ← אינדקס המורה
```

**כל שלושת העותקים נושאים PII מלא** — שם הקטין, מייל ההורה. זה מקור לבאגי
מחיקה חוזרים (§7).

`USER_DATA_PATHS` הוא **מקור אמת יחיד** לרשימת מה שנמחק במחיקת משתמש, עם שני
צרכנים: `deleteAllUserData()` ו-`resetSystem()`. `validate.sh` אוכף שהוא מוגדר
פעם אחת בלבד ואוסר על שמות חלופיים — כי בעבר היו שלוש רשימות שנפרדו זו מזו.

---

## 5. מודל האבטחה

**הכל ב-`database.rules.json`.** הדפוס: `.write` ברמת הבעלים, ו-`.validate`
ברמת השדה למה שהבעלים לא אמור לקבוע על עצמו:

```json
"teachers": { "$teacherUid": {
  ".write": "auth.uid == $teacherUid || auth.token.email == '<admin>'",
  "verified": { ".validate": "<admin> || newData.val() == false" },
  "ageVerification": {
    "status": { ".validate": "<admin> || newData.val() != 'verified'" }
  }
}}
```

**חברות בשיחה נגזרת מה-`chatId` עצמו**, לא מצומת נפרד:

```json
"messages": { "$chatId": {
  ".read": "$chatId.beginsWith(auth.uid + '_') || $chatId.endsWith('_' + auth.uid)"
}}
```

זה תיקון לחור שבו החברות נגזרה מ-`chatParticipants` — צומת שכל מחובר יכול היה
לכתוב אליו, ולכן כל אחד קרא כל שיחה פרטית בין קטינים.

**46 טסטי חוקים** רצים מול אמולטור Firebase עם קובץ החוקים האמיתי
(`npm run test:rules`). זה נבנה אחרי שבאג מחיקה חזר **ארבע פעמים** — הקוד קרא
צמתים שהחוקים אסרו, הדחייה נבלעה ב-`catch`, והמחיקה "הצליחה". טסט מעל ה-JS
לא יכול לראות את זה.

**סיבוב ביקורת חיצונית (5–6.9.2026):** מהנדס חיצוני עבר על הפרויקט ומצא ארבעה
חורים נוספים, כולם מאותו דפוס — שדה שהנבדק שולט בו קובע החלטה עליו:
`users/<uid>/parentConsentAt` (קטין יכול היה לכתוב על עצמו שההורה אישר, והמנהל
מאשר בהנחת שווא), `schedules/<uid>` (כל מחובר קרא מתי כל קטין אחר פנוי/בבית),
`messageStats` ו-`referrals` (כל מחובר יכול היה לזייף עבור מישהו אחר). כל
הארבעה נסגרו בחוקים ותועדו בטסטים חדשים שנכשלים על החוקים הקודמים ועוברים על
החדשים. שתי טענות נוספות מאותה ביקורת נבדקו ונמצאו **לא נכונות**: פרטי הורה
ומסמכי זיהוי אינם תחת הצומת הציבורי `teachers` — הם ב-`users/<uid>` וב-
`teacherVerification/<uid>`, שניהם owner-only.

---

## 6. CI והשערים

| Workflow | מתי | מה |
|---|---|---|
| `test.yml` | כל PR/push | 18 טסטי e2e (Playwright) + 46 טסטי חוקים |
| `deploy.yml` | push ל-main (למעט `**.md`) | טסטים → `validate.sh` → Netlify |
| `deploy-firebase-rules.yml` | שינוי בחוקים | פריסה אוטומטית |
| `weekly-parent-reports.yml` | ראשון 06:00 UTC | דוח להורים; כישלון פותח Issue |
| `build-android.yml` / `build-ios.yml` | ידני | AAB חתום / בנייה לסימולטור |

**`scripts/validate.sh`** — ולידטור "חוקה" שאוכף כללי פרויקט שאין דרך אחרת
לאכוף: שם המותג, הגרדיאנט המוגן, `COMMISSION_RATE=0`, קבוע מחיר יחיד, חמש
כותרות אבטחה, ושמקום קריאה אחד בלבד מנווט למסך הדרילס. רץ גם כ-pre-commit hook.

**⚠️ אין CSP** — מודע ומתועד. קובץ יחיד עם סקריפטים inline ענקיים; כל מדיניות
ריאלית תדרוש `unsafe-inline` ותנטרל את עצמה. שווה רק יחד עם הוצאת הסקריפטים
לקובץ נפרד, וזה שינוי גדול.

**`netlify/functions/ai-assistant.js` תוקן (6.9.2026):** עד לתיקון קיבלה כל
בקשת POST מכל מקור באינטרנט — ללא אימות, `Access-Control-Allow-Origin: '*'`,
בלי הגבלת קצב אמיתית. עכשיו דורשת טוקן Firebase תקין (`admin.auth().
verifyIdToken`) ומגבילה ל-20 קריאות/שעה/`uid`, באותו דפוס בדיוק כמו
`send-push.js`. אורח אנונימי עדיין יכול להשתמש; בוט שפוגע ישירות ב-URL בלי
session — לא.

---

## 7. חולשות ידועות — הרשימה הכנה

### דפוסים שחזרו, ולכן שווה לחשוד בהם שוב

**1. `catch` ריק שבולע דחיית הרשאה.** באג מחיקת המשתמש חזר ארבע פעמים בגלל
זה. הקוד קרא צומת שהחוקים אסרו, השגיאה נבלעה, והמסך הציג "✅ נמחק". הפעם
האחרונה הייתה סתם **שם שדה שגוי** (`uid` במקום `from`) — הלולאה רצה, מצאה אפס
התאמות, ו"הצליחה".

**2. בדיקה בצד לקוח שקוראת שדה שהנבדק שולט בו.** קרה פעמיים: פעם ב-
`chatParticipants`, ופעם ב-`ageVerification.status` (מורה יכול היה לסמן את
עצמו כמאומת, והמנהל אישר אותו בהנחה שמישהו בדק מסמך). שניהם תוקנו בחוקים.
**אם תמצא שומר נוסף מהסוג הזה — זה ממצא אמיתי.**

**3. `icon()` שמחזיר מחרוזת ריקה לשם לא קיים.** 88 קריאות רינדרו כלום, בשקט,
לאורך חודשים. תוקן, אבל הדפוס — כשל שקט במקום שגיאה — קיים במקומות נוספים.

### פערים פתוחים

| מה | חומרה | הערה |
|---|---|---|
| **מודרציית UGC בקנה מידה** | גבוה | קיים: סינון שפה, זיהוי שיתוף פרטי קשר (מנותב לתור `reports` הקבוע, לא לפעמון notifications שנחתך אחרי 40 פריטים), כפתור דיווח, תור אישור למנהל, מייל חד-פעמי לשני ההורים כשנפתחת שיחה חדשה, המלצת נוכחות הורה + הקלטה בהצטרפות לשיעור. אין: בדיקה אנושית שיטתית, וגילוי ההודעות הוא regex/keyword בלבד — לא context-aware. |
| **הודעות קוליות אינן נסרקות** | גבוה | אי אפשר regex על אודיו. דורש תמלול — פיצ'ר בפני עצמו. |
| **מצב לילה** | נמוך-בינוני | תוקן חלקית (5.9.2026): 16 סלקטורים סגרו את 207 המופעים שהיו בלתי נראים לגמרי (יחס 1.02:1). נשארו כ-560 מופעים ביחס 2.3–4.5 — קריא אך עמום, לא ברמת הפלטה הרגילה. אין `prefers-color-scheme`. |
| **74 מתוך 102 שדות טופס בלי `value=` binding** | בינוני | `render()` בונה מחדש את כל ה-`innerHTML`; טקסט שהוקלד אובד. בפועל אף מאזין חי לא קורא ל-`render()`, אז הטריגר צר — אבל הפוטנציאל שם. |
| **`FIREBASE_PRIVATE_KEY` ב-Netlify אינו מסומן secret** | בינוני | קריא בטקסט גלוי דרך ה-API. סימונו כסודי מפצל אותו לחמישה הקשרי פריסה = 8,660 בתים, מעל מגבלת 4KB של Lambda ושובר את כל הפריסות. מצב ביניים מודע. |
| **`uuid` <11.1.1 בתוך `firebase-admin`** | נמוך | הפגיעות דורשת העברת `buf` ל-uuid v3/v5/v6 — API שהקוד לא נוגע בו. התיקון היחיד הוא שדרוג major. |
| **אין CSP** | — | ראה §6. |

### מגבלה תפעולית שמעצבת את זרימת העבודה

**פריסת ייצור = 15 קרדיטים מתוך 3,000 לחודש.** בחשבונית האחרונה 194 פריסות
לקחו 2,910 — **97% מהתקציב** — בעוד רוחב פס, פונקציות ובקשות יחד לקחו פחות
משלושה. התקרה היא 200 פריסות בחודש. **התקציב נמדד בפריסות, לא בשימוש.**

---

## 8. איפה שווה להסתכל קודם

אם יש לך שעה, לפי סדר תשואה:

1. **`database.rules.json`** — זו כל שכבת האבטחה. 356 שורות. אם משהו שם שגוי,
   זה שגוי בייצור.
2. **`tests/rules/database.rules.test.js`** — 46 טסטים שמתעדים מה נשבר בעבר.
   קריאה שלהם מספרת את ההיסטוריה של הפרויקט מהר יותר מכל מסמך.
3. **`deleteAllUserData()` ב-`index.html`** — הפונקציה שחזרה ארבע פעמים.
   אם יש עוד נתיב עם PII שהיא לא מכסה, זה ממצא.
4. **`netlify/functions/parent-consent.js`** — 176 שורות שמשחררות חשבון של
   קטין. הנקודה הרגישה ביותר במערכת.
5. **`scripts/validate.sh`** — מה הפרויקט מחשיב כבלתי-ניתן-לשבירה.

**מה שכנראה לא שווה את הזמן:** לקרוא את 14 אלף השורות של `index.html` ברצף.
הן ארוכות אבל שטוחות — רובן רינדור של מסכים.

---

## 9. שאלות שהייתי שמח לדעת עליהן דעה חיצונית

1. **האם `.validate` ברמת השדה הוא הגנה מספקת** במודל שבו הלקוח כותב ישירות
   ל-DB, או שצריך להעביר כתיבות רגישות לפונקציות שרת?
2. **50 צמתים בשורש עם שכפול ידני לאינדקסים** — האם זה יחזיק ב-500 מורים,
   או שהמעבר ל-Firestore בלתי נמנע?
3. **מודרציית UGC:** מה המינימום המעשי לפלטפורמה שכל משתמשיה קטינים, בלי צוות
   מודרציה?
4. **`render()` שבונה מחדש את כל ה-DOM** — האם שווה להשקיע ברינדור ממוקד לפני
   שיש משתמשים, או שזו אופטימיזציה מוקדמת?
5. **הודעות קוליות** — יש דרך זולה יותר מתמלול מלא לזהות שיתוף מספר טלפון?
6. **פיקוח על השיעור עצמו:** השיעורים מתקיימים דרך קישור Zoom/Meet חיצוני
   שהמורה בוחר — לא מוקלט, לא מפוקח ע"י הפלטפורמה. התוספת האחרונה (6.9.2026)
   היא המלצה להורה להיות נוכח ולשקול הקלטה + מייל אוטומטי בכל הצטרפות
   ובפתיחת שיחה חדשה. מול פלטפורמות דומות (Outschool מקליטה כל שיעור אוטומטית
   ומחייבת check-in בווידאו, Yubo עושה אמידת גיל בפרצוף) — האם המלצה בלבד,
   בלי אכיפה טכנית, מספיקה כאן, או שצריך להעביר שיעורים לשכבת וידאו בתוך
   האפליקציה כדי שההקלטה תהיה ברירת מחדל ולא תלויה בהורה שנזכר?

---

## 10. מה שנבדק ואומת (כדי לא לחזור על זה)

- **כל האינדקסים ב-RTDB קיימים ונכונים** — אין שאילתה חסרת אינדקס.
- **אין סודות בריפו** מעבר למפתח ה-Web של Firebase, שציבורי בהגדרה.
- **RTL מוקפד** — נבדק; הבאגים האמיתיים מצומצמים לזנבות בועות צ'אט ותצוגת טלפון.
- **כותרות אבטחה מוגשות בפועל** — securityheaders.com מדרג A.
- **שער הצ'אט, אימות הגיל, אישור ההורה** — כולם נסגרו ונעולים בטסטים.
