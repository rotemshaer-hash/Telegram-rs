{
  "name": "drushe",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:rules": "firebase emulators:exec --only database --project demo-drushe \"node --test tests/rules/*.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@playwright/test": "^1.61.1",
    "firebase-tools": "^15.28.2"
  }
}
